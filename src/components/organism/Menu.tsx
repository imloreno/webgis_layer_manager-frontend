import AddLayerForm from "@molecules/AddLayerForm"

const Menu = () => {
   return (
      <nav>
         This is the real menú
         <AddLayerForm isOpen={false} />
      </nav>
   )
}

export default Menu